<!-- sidebar -->
<%= render 'admin/sidebar' %>

<!-- table -->

<div class="row">
  <div class="col-md-10 offset-md-2 mb-4">
    <div class="card">
      <h5 class="card-header">評価進捗区分登録</h5>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th scope="col">ID</th>
                <th scope="col">ステータス</th>
                <th scope="col">作成日時</th>
                <th scope="col">更新日時</th>
                <th scope="col">削除フラグ</th>
              </tr>
            </thead>
            <tbody>
              <% @evaluator_progresses.each do |i| %>
                <tr>
                  <th scope="row"><%= i.id %></th>
                  <td><%= i.status %></td>
                  <td><%= i.created_at.strftime('%Y/%m/%d') %></td>
                  <td><%= i.updated_at.strftime('%Y/%m/%d') %></td>
                  <td><%= i.deletion_flag %></td>
                  <td><%= link_to '編集', "/admin/evaluator_progresses/#{i.id}/edit", class: 'btn btn-sm btn-primary' %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
          <%= link_to '新規作成', "/admin/evaluator_progresses/new", class: 'btn btn-block btn-secondary' %>
      </div>
    </div>
  </div>
</div>
