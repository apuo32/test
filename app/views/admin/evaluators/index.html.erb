<!-- sidebar -->
<%= render 'admin/sidebar' %>

<!-- table -->

<div class="row">
  <div class="col-md-10 offset-md-2 mb-4">
    <div class="card">
      <h5 class="card-header">評価者区分登録</h5>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th scope="col">ID</th>
                <th scope="col">ステータス</th>
                <th scope="col">作成日時</th>
                <th scope="col">更新日時</th>
                <th scope="col">削除フラグ</th>
                <th scope="col">削除日時</th>
              </tr>
            </thead>
            <tbody>
              <% @evaluators.each do |evaluator| %>
                <tr>
                  <th scope="row"><%= evaluator.id %></th>
                  <td><%= evaluator.status %></td>
                  <td><%= evaluator.created_at.strftime('%Y/%m/%d') %></td>
                  <td><%= evaluator.updated_at.strftime('%Y/%m/%d') %></td>
                  <td><%= evaluator.deletion_flag %></td>
                  <td><%= evaluator.deletion_date ? evaluator.deletion_date.strftime('%Y/%m/%d') : '-' %></td>
                  <td><%= link_to '編集', edit_admin_evaluator_path(evaluator), class: 'btn btn-sm btn-primary' %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
          <a href="/admin/evaluators/new" class="btn btn-block btn-secondary">新規作成</a>
      </div>
    </div>
  </div>
</div>
